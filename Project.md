Project.md – финальное состояние (рефакторинг + улучшенное мобильное управление)

## Движок и технологии
- Чистый JavaScript (ES6+) + HTML5 Canvas.
- **Модульная архитектура** – ES6 modules.
- **Конечный автомат**, **Event Bus**, **AssetManager**, **чистые функции**, **внедрение зависимостей**.
- **Уровни в JSON**, **унификация анимаций**, **выборочное тестирование**.
- **Современное мобильное управление** – виртуальный джойстик и кнопка прыжка.

## Игровой процесс
2D платформер-раннер. Игрок собирает колёса, достигает правого края → следующий уровень. Падение → Game Over с рестартом. Управление: клавиатура (для ПК) или сенсорный джойстик + кнопка (для мобильных).

## Реализованные улучшения

### Архитектурные (пункты 1-10)
1. ✅ Переход на модули ES6
2. ✅ Чистые функции
3. ✅ AssetManager
4. ✅ Event Bus
5. ✅ Конечный автомат
6. ✅ Внедрение зависимостей (gameStore)
7. ✅ Разделение на слои (Core, Game, UI) – выполнено на 90%
8. ✅ Хранение уровней в JSON
9. ✅ Унификация анимаций (SpriteAnimator)
10. ✅ Выборочное тестирование

### Новое мобильное управление
- **Виртуальный джойстик** (`ui/VirtualJoystick.js`) – аналоговое управление движением влево/вправо с мёртвой зоной.
- **Кнопка прыжка** (`ui/JumpButton.js`) – с визуальной обратной связью (прозрачность).
- Динамическая адаптация позиций элементов при изменении размера экрана или повороте.
- Отрисовка джойстика и кнопки через `drawUI()`.

## Структура проекта (финальная)
bicycle-runner/
├── index.html
├── css/styles.css
├── js/
│ ├── config.js
│ ├── main-module.js
│ ├── game.js
│ ├── world.js
│ ├── player.js
│ ├── camera.js
│ ├── collision.js
│ ├── controls.js
│ ├── core/
│ │ ├── assetManager.js
│ │ ├── canvas.js
│ │ ├── layers.js
│ │ ├── eventBus.js
│ │ ├── stateMachine.js
│ │ ├── gameStore.js
│ │ └── spriteAnimator.js
│ ├── entities/
│ │ ├── platform.js
│ │ └── wheel.js
│ ├── scenery/
│ │ ├── skybackground.js
│ │ ├── sky.js
│ │ ├── background.js
│ │ └── decorations.js
│ ├── ui/
│ │ ├── VirtualJoystick.js
│ │ └── JumpButton.js
│ ├── levels/
│ │ ├── level1.json
│ │ └── level2.json
│ └── utils/math.js
└── tests/
└── runTests.js

text

## Запуск
- Поддерживаются мобильные устройства и ПК (клавиатура).
- Для мобильных автоматически включается сенсорный джойстик и кнопка.
- Для ПК – клавиши стрелки/A/D, пробел.

## Последние изменения
- ✅ Полностью переработано мобильное управление (вместо заглушки).
- ✅ Добавлены классы `VirtualJoystick` и `JumpButton`.
- ✅ Обработчики событий `resize` и `orientationchange` для адаптации UI.
- ✅ Удалены старые примитивные тач-зоны.

## Тестирование
- Тесты (`tests/runTests.js`) проверяют коллизии, генерацию колёс, аниматор.
- Горизонтальные тесты скорректированы и проходят.

## Заключение
Проект полностью рефакторирован, архитектура чистая, управление удобное. Готов к публикации и дальнейшему расширению (звуки, сохранения, редактор уровней).

---

*Версия 3.0. Завершено 07.04.2026.*