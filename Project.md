# Project.md – актуальное состояние после внедрения Event Bus

## Движок и технологии
- Чистый JavaScript (ES6+) + HTML5 Canvas, без фреймворков.
- **Модульная архитектура** – вся логика разбита на ES6 модули (import/export).
- **Событийная шина (Event Bus)** – связь между модулями через события.
- **AssetManager** – централизованная загрузка ресурсов.
- Адаптация под мобильные устройства, предзагрузка ресурсов, ограничение dt.
- Централизованная конфигурация в `config.js`.

## Игровой процесс
2D платформер-раннер. Игрок собирает колёса, достигает правого края → следующий уровень. Падение → Game Over с рестартом.

## Архитектурные решения

### Модули ES6
- Все файлы (кроме `config.js`) – модули с `export`/`import`.
- Точка входа – `main-module.js` (подключается как `<script type="module">`).

### Event Bus
- Модуль `core/eventBus.js` предоставляет методы `on`, `off`, `emit`.
- Используется для:
  - `level.complete` – переключение уровня через задержку.
  - `game.over` – показ UI окончания игры.
  - `wheel.collected` – обновление счётчика (готово для будущего).
- Игровой цикл больше не содержит логики проверки `state.gameOver` – всё через события.

### AssetManager
- Загружает и кеширует все изображения (игрок, фоны, камни, шины, колёса).
- Предоставляет доступ через `assetManager.get(key)`.
- Сохраняет обратную совместимость (записывает в `window` там, где это ещё нужно).

## Структура проекта (модульная)
bicycle-runner/
├── index.html
├── css/styles.css
├── js/
│ ├── config.js # глобальные константы
│ ├── main-module.js # точка входа, подписки на события
│ ├── game.js # canvas, ctx, gameOverUI
│ ├── world.js # управление уровнем и фоном
│ ├── player.js # игрок (генерирует события)
│ ├── camera.js # камера
│ ├── collision.js # коллизии
│ ├── controls.js # управление
│ ├── core/
│ │ ├── assetManager.js
│ │ ├── canvas.js
│ │ ├── layers.js
│ │ └── eventBus.js
│ ├── entities/
│ │ ├── platform.js
│ │ └── wheel.js
│ ├── scenery/
│ │ ├── skybackground.js
│ │ ├── sky.js
│ │ ├── background.js
│ │ └── decorations.js
│ ├── levels/
│ │ ├── level1.js
│ │ └── level2.js
│ └── utils/math.js
└── assets/ (изображения)

text

## Ключевые особенности

### Динамический фон
- `background.js` загружает разные текстуры (горы, заводы) и масштабирует их.
- Уровни содержат поле `backgroundImage`, которое используется в `world.setLevel()`.

### Декорации (камни / шины)
- На втором уровне (`level.number === 2`) отрисовываются шины (масштаб 0.2, больше объектов).
- На первом – камни.

### Цвет неба
- Уровни могут определять `skyColor`. По умолчанию – `CONFIG.DEFAULT_SKY_COLOR` (чёрный).

### Управление и отладка
- Клавиатура (стрелки/A/D, пробел) + тач-зоны.
- Клавиша `ё` включает хитбокс и отладочную информацию.

## Запуск
1. Убедиться, что в `index.html` подключены только `config.js` и `main-module.js` (как модуль).
2. Запустить Live Server.
3. Игра работает полностью на событиях и модулях.

## Последние изменения
- **Event Bus** – полностью заменил прямые вызовы `gameOverUI.show()` и таймауты.
- **Рефакторинг `player.js` и `main-module.js`** – удалена старая логика из игрового цикла.
- **Подписки на события** – `level.complete` управляет переключением уровней, `game.over` – показом UI.
- **Счётчик колёс** – готов к переходу на событие `wheel.collected` (пока оставлен прямой доступ).

---

*Актуально на 04.04.2026. Версия стабильна, архитектура готова к дальнейшему расширению.*